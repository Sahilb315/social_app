import 'package:flutter/material.dart';

class ProfileRepliesTabPage extends StatelessWidget {
  const ProfileRepliesTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.headphones),
    );
  }
}
